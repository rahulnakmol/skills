---
"rahulnakmol-skills": minor
---

Add `spotlight`, a productivity skill that turns established context into the smallest source-grounded visual needed for a human decision.

The skill separates observed, inferred, and proposed claims. It starts with portable inline forms and loads standalone HTML guidance only when the subject needs a richer artifact. The output keeps sources, omissions, unknowns, and verification beside the view so visual polish cannot substitute for evidence.

The catalog, documentation site, wiki, plugin manifest, and routing table now include the skill. Historical routing scorecards remain unchanged as records of the model runs that produced them; offline checks validate each scorecard against its own results while current catalog coverage remains a separate requirement.

Enhance `press` so every branded HTML document derives a clickable, nested table of contents from its section headings. Headings receive stable, unique anchors, including numbered anchors when labels repeat. The renderer remains self-contained and script-free.
