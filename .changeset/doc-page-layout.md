---
"rahulnakmol-skills": patch
---

Give the four standalone documentation pages a layout of their own. How it fits, the example, the tools reference, and the leaders page were plain markdown on the site's bare shell, which supplies no container, no reading measure, and no vertical rhythm, because the stylesheet zeroes prose margins globally and every other layout restores spacing through its own wrapper. A new page layout supplies the container, a header built from frontmatter, and the table-of-contents rail, and the stylesheet gains the document typography the site never had, including table, rule, and quote styling. How it fits drops its page-local stylesheet: its flow diagrams, stage blocks, and note box are now shared components, both figures are redrawn so their labels scale with the page and their captions can no longer be clipped, and the eight handoffs read as a numbered rail. The tools page states each mechanism in the existing tool components and closes with a table of where enforcement lands, and the example page's features and altitude map become tables. Paragraphs inside a tool card also gain the spacing they never had, which applies to every skill page.
