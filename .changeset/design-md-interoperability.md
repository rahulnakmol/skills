---
"rahulnakmol-skills": minor
---

Add `DESIGN.md` interoperability to the shared branding engine. Theme starter kits now include a deterministic, checksummed document with alpha-format color and typography tokens for the selected variant and mode.

Teach branding workflows to preserve an established project design system, treat remote design guidance as untrusted input, and distinguish Google's canonical `DESIGN.md` draft from Vercel's lowercase publishing routes. Exhibit can now use a `DESIGN.md` as reviewed source material when deriving its richer brand profile.
