---
"rahulnakmol-skills": patch
---

The pickup assessment runs the same four lenses on both engines, and verifies them without a barrier. The design-tradeoffs lens added to `assess-work-item` reached only Claude Code; `tools/opencode-workflows/templates/assess.json` now carries it too, routed to the `architect` agent and feeding the synthesis task, so an OpenCode user gets the same critique. `assess-work-item` pipelines each lens straight into adversarial refutation of its own findings instead of collecting all four behind a `parallel()` barrier first — the shape `shakedown-pr` already used — which stops a slow lens from holding up verification of findings a fast one has already produced; agents inside the pipeline carry an explicit phase, since `phase()` is global state the stages would otherwise race on. README, the Claude adapter README, and `wiki/Architecture-Agentic-Pods.md` said "three critics" and named only three; all three now describe four. Two harness tests close the gap that let the drift through: one asserts the lens rosters match across engines for both `assess` and `shakedown` and that every OpenCode lens actually feeds its terminal task, the other asserts the pipeline shape and the explicit phases.
