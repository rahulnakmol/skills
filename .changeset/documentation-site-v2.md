---
"rahulnakmol-skills": minor
---

Rebuild every documentation page as a guided walkthrough. Each skill page now opens with what the skill is and is not, shows its procedure as a step flow, names one realistic scenario and threads it through a working example, gives five per-tool invocations with copyable prompt cards for Claude Code, OpenCode, Cursor, Codex, and GitHub Copilot, answers common questions from the repository's own history in a collapsible list, and closes with observable signs the skill is working and where it sits among its siblings. Group pages become narrative landings with a thesis, a named failure pattern, and an ordered path through their skills. A new How it fits guide walks one product from idea through epics, features, and user stories in a tracker to the gate ledger, the audited pull request, and the benefits report. The install reference explains every command and covers per-tool and single-skill installs, a Tools page carries setup for all five tools, navigation lists user-invoked skills first, the theme toggle is fixed, and every claim about third-party tools is restated as a fact about what this repository ships. The page contract is enforced by tests, including a re-render binding for the press example and a scenario requirement no new skill page can merge without.
