# Cursor adapter

# Cursor adapter

- Use Cursor **auto** mode for orchestration; multitask for parallel read lanes
- Model IDs resolve via `skills/developer/model-routing/models.md` locally

Install: `../../scripts/install-adapters.sh --tool cursor`
