# Out of scope 0001: No caveman/cavecrew vendoring

Third-party packs (cavecrew, caveman) are installed via `skills.manifest.yaml` in dotfiles, never copied into this repo.

OpenCode `caveman-*` commands remain in `adapters/opencode/commands/` only as thin compatibility shims until removed upstream.
