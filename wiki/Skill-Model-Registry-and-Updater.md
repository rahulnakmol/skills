# Model registry and updater

Canonical: `skills/developer/model-routing/models.md`.

Policy: Anthropic, OpenAI, Google published defaults.

`update-models` skill + monthly `update-models.yml` workflow.
