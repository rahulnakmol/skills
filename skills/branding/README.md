# Branding group

Identity — company or personal — applied to everything an agent produces: tone of voice, color and palette, storytelling. First shipped skill: `press`, which renders an approved markdown document to a branded, self-contained HTML page and a PDF. Bring your own tokens via `PALETTE.md` overrides.

The group installs on its own and references no other group.
