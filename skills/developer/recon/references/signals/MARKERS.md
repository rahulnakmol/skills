# Signal markers

| Marker family | Examples |
|---------------|----------|
| Mainframe | `.cbl`, `.jcl`, CICS bundles |
| AS400 | `**/*.rpgle`, `QSYS` paths |
| Integration | `mule-artifact.json`, MQ queues |
| Monolith | single deployable, layered packages |
| Microservices | `docker-compose` service matrix, service mesh |
| Event | `kafka`, `schema-registry`, outbox tables |
| Monorepo | `WORKSPACE`, `nx.json`, `settings.gradle` |
| Native UI | `.csproj` WPF, `CMakeLists` + Qt |
| Web | `vite.config`, Next.js app dir |
| Strangler | legacy + modern routes side-by-side |
