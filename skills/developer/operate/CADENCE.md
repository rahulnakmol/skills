# Maintenance cadence

The schedule the maintenance lane of this charter runs on. The cadence holds when nothing is broken: maintenance that runs only after an incident is incident response under another name, and it leaves dependency and debt work to accumulate between incidents.

- Weekly dependency review
- Monthly debt burn slot
- Findings re-enter via `slice` as CI items
