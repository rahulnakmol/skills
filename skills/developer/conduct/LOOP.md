# Loop mode

- Single writer, single verifier, tight iteration
- SPEC-TS ledger advances gate-by-gate
- Verification decomposition stays in one grit gate ledger; tree layers are sections of that ledger, not additional agents (`grit/METHOD.md`)
- Use `tools/opencode-workflows` templates when headless
