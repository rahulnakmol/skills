# Example GitHub Actions workflow snippet for PR shakedown
name: pr-shakedown
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  shakedown:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and test
        run: |
          npm ci
          npm test
